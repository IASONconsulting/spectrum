const bar=document.getElementById("progressBar"),button=document.getElementById("redirectButton");function redirect(){window.location.href="idpms://"}document.addEventListener("DOMContentLoaded",()=>{bar.addEventListener("animationend",e=>{"fillBar"===e.animationName&&redirect()}),button.addEventListener("click",redirect)});